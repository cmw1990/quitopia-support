const puppeteer = require("puppeteer"); (async () => { const browser = await puppeteer.launch({headless: false}); const page = await browser.newPage(); await page.setViewport({width: 1280, height: 800}); await page.goto("http://localhost:6001/easier-focus/task-manager", {waitUntil: "networkidle2", timeout: 15000}); await page.waitForTimeout(3000); await page.screenshot({path: "taskmanager.png"}); console.log("Screenshot saved"); await browser.close(); })().catch(err => { console.error("Error:", err); process.exit(1); });
